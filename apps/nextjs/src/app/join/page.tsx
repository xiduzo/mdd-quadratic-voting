import type { NextPage } from "next";

export const runtime = "edge";

const JoinPage: NextPage = () => {
  return (
    <main>
      <div>join page form</div>
    </main>
  );
};

export default JoinPage;
